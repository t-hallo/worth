<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title>{{ title }} // WORTH.EE</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <!-- 引入 Inter 字体，对比 Space Grotesk 更具系统文档的冷静感 -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500&family=Syne:wght@800&display=swap" rel="stylesheet">
    <style>
        body { font-family: 'Inter', sans-serif; background-color: #f7f7f7; display: flex; align-items: center; justify-content: center; min-height: 100vh; margin: 0; }
        .font-syne { font-family: 'Syne', sans-serif; }
        .no-scrollbar::-webkit-scrollbar { display: none; }
        
        #outerWrap {
            width: 95%; max-width: 850px; height: 90vh;
            border: 1px solid #111; display: flex; flex-direction: row;
            background: white; overflow: hidden;
        }

        @media (max-width: 768px) {
            body { padding: 0; }
            #outerWrap { width: 100%; height: 100vh; border: none; flex-direction: column; }
            #sidebar { display: none; }
        }

        /* 正文紧凑化排版 */
        .article-body { font-size: 0.95rem; line-height: 1.6; color: #111; letter-spacing: -0.01em; }
        .article-body p { margin-bottom: 0.8rem; }
    </style>
</head>
<body class="selection:bg-[#111] selection:text-white overflow-hidden">

<div id="outerWrap">
    <!-- Sidebar -->
    <aside id="sidebar" class="w-[180px] border-r border-[#111] p-5 flex flex-col shrink-0 bg-white">
        <!-- 修复：点击标题回首页 -->
        <a href="/" class="font-syne text-md font-black mb-6 text-[#111] no-underline">WORTH.EE</a>
        
        <div class="text-[8px] font-mono text-neutral-400 mb-4 tracking-widest">[INDEX]</div>
        <div class="flex flex-col gap-2 font-mono text-[9px]">
            {% for post in collections.posts %}
            <a href="{{ post.url }}" class="{% if page.url == post.url %}text-[#111] font-bold{% else %}text-neutral-400 hover:text-[#111]{% endif %} no-underline">
                {{ loop.index | idx }} // {{ post.data.title | truncate(15) }}
            </a>
            {% endfor %}
        </div>
    </aside>

    <!-- Main -->
    <main class="flex-1 flex flex-col overflow-hidden">
        <header class="flex w-full border-b border-[#111] h-10 items-center justify-between px-5 shrink-0 text-[8px] font-mono text-neutral-400">
            <div>[{{ category or "POST" }}]</div>
            <div id="clock">00:00:00</div>
        </header>

        <div class="flex-1 overflow-y-auto no-scrollbar p-6">
            <div class="w-full">
                <h1 class="font-syne text-3xl font-black tracking-tight leading-[0.9] mb-6 text-[#111]">
                    {{ title }}
                </h1>
                <div class="article-body">
                    {{ content | safe }}
                </div>
            </div>
        </div>

        <footer class="h-8 border-t border-[#111] flex items-center px-5 text-[8px] font-mono text-neutral-400 justify-between">
            <div>{{ date | date("yyyy.MM.dd") }}</div>
            <!-- 修复：点击 INDEX 回首页 -->
            <a href="/" class="text-[#111] hover:underline no-underline uppercase tracking-widest">[▲ INDEX]</a>
        </footer>
    </main>
</div>

<script>
    function updateClock() {
        document.getElementById('clock').textContent = new Date().toTimeString().split(' ')[0];
    }
    setInterval(updateClock, 1000); updateClock();
</script>
</body>
</html>
